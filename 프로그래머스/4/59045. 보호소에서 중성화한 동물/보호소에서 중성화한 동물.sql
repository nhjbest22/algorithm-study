WITH O AS (
    SELECT 
        * 
    FROM
        ANIMAL_OUTS
    WHERE
        SEX_UPON_OUTCOME NOT LIKE 'Intact%'
)

SELECT
    I.ANIMAL_ID,
    I.ANIMAL_TYPE,
    I.NAME
FROM
    ANIMAL_INS I
JOIN
    O
ON
    I.ANIMAL_ID = O.ANIMAL_ID
    AND
    I.SEX_UPON_INTAKE LIKE 'Intact%'
ORDER BY
    I.ANIMAL_ID ASC;