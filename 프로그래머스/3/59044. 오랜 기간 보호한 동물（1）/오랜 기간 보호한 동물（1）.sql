WITH O AS (
    SELECT
        *
    FROM
        ANIMAL_OUTS
)

SELECT
    I.NAME,
    I.DATETIME
FROM
    ANIMAL_INS I
LEFT JOIN
    O
ON
    I.ANIMAL_ID = O.ANIMAL_ID
WHERE
    O.ANIMAL_ID IS NULL
ORDER BY
    I.DATETIME ASC
FETCH FIRST 3 ROWS ONLY;